# Nolan

WoL in Java. For testing purposes.

The project name has now been tainted.


### Usage

Set the WOLMAC env variable to the desired MAC address in the format `0A:1B:2C:3D:4E:5F` for it to work.

I used gradle because java building is dumb.

Run with `gradle run`

Get jar with `gradle jar`
